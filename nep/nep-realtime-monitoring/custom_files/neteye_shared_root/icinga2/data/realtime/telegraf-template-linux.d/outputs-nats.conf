# # Send telegraf measurements to NATS
[[outputs.nats]]
   ## URLs of NATS servers
   servers = ["nats://nats-server.neteyelocal:4222"]

   ## Optional credentials
   # username = ""
   # password = ""

   ## Optional NATS 2.0 and NATS NGS compatible user credentials
   # credentials = "/etc/telegraf/nats.creds"

   ## NATS subject for producer messages
   subject = "telegraf.metrics"

   ## Use Transport Layer Security
   secure = true

   ## Optional TLS Config
   tls_ca = "/neteye/local/telegraf/conf/certs/neteyecloud-test/root-ca.crt"
   tls_cert = "/neteye/local/telegraf/conf/certs/neteyecloud-test/telegraf_wo.crt.pem"
   tls_key = "/neteye/local/telegraf/conf/certs/neteyecloud-test/private/telegraf_wo.key.pem"
   ## Use TLS but skip chain & host verification
   # insecure_skip_verify = false

   ## Data format to output.
   ## Each data format has its own unique set of configuration options, read
   ## more about them here:
   ## https://github.com/influxdata/telegraf/blob/master/docs/DATA_FORMATS_OUTPUT.md
   data_format = "influx"
